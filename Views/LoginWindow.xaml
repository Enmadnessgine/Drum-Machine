<Window x:Class="Drum_Machine.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Drum Machine - Авторизація" 
        Height="450" Width="380" 
        WindowStartupLocation="CenterScreen"
        Background="#1E1E1E" 
        ResizeMode="NoResize"
        WindowStyle="ThreeDBorderWindow">

    <Grid Margin="30">
        <StackPanel VerticalAlignment="Center">

            <TextBlock x:Name="txtTitle" 
                       Text="Вхід" 
                       Foreground="White" 
                       FontSize="28" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,30"/>

            <TextBlock Text="Логін" Foreground="#AAAAAA" Margin="0,0,0,5" FontSize="14"/>
            <Border CornerRadius="5" Background="#2D2D2D" Margin="0,0,0,20">
                <TextBox x:Name="txtUsername" 
                         Height="35" 
                         Background="Transparent" 
                         Foreground="White" 
                         BorderThickness="0" 
                         VerticalContentAlignment="Center" 
                         Padding="10,0"
                         FontSize="14"/>
            </Border>

            <TextBlock Text="Пароль" Foreground="#AAAAAA" Margin="0,0,0,5" FontSize="14"/>
            <Border CornerRadius="5" Background="#2D2D2D" Margin="0,0,0,30">
                <PasswordBox x:Name="txtPassword" 
                             Height="35" 
                             Background="Transparent" 
                             Foreground="White" 
                             BorderThickness="0" 
                             VerticalContentAlignment="Center" 
                             Padding="10,0"
                             FontSize="14"/>
            </Border>

            <Button x:Name="btnMain" 
                    Content="Увійти" 
                    Click="btnMain_Click" 
                    Height="40" 
                    Background="#4CAF50" 
                    Foreground="White" 
                    FontWeight="Bold" 
                    FontSize="16"
                    Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button x:Name="btnSwitch" 
                    Content="Немає акаунту? Реєстрація" 
                    Click="SwitchMode_Click" 
                    Margin="0,15,0,0" 
                    Background="Transparent" 
                    Foreground="#81C784" 
                    BorderThickness="0" 
                    Cursor="Hand"
                    HorizontalAlignment="Center">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

        </StackPanel>
    </Grid>
</Window>